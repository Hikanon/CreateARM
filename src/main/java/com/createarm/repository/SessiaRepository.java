package com.createarm.repository;

import com.createarm.model.Sessia;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SessiaRepository extends JpaRepository<Sessia, Integer> {
}
