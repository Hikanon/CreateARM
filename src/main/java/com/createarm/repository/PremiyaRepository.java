package com.createarm.repository;

import com.createarm.model.Premiya;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PremiyaRepository extends JpaRepository<Premiya, Integer> {
}
