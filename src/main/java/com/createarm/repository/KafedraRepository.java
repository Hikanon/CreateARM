package com.createarm.repository;

import com.createarm.model.Kafedra;
import org.springframework.data.jpa.repository.JpaRepository;

public interface KafedraRepository extends JpaRepository<Kafedra, Integer> {
}
