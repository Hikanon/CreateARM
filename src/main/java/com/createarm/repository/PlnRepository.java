package com.createarm.repository;

import com.createarm.model.Pln;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlnRepository extends JpaRepository<Pln, Integer> {
}
