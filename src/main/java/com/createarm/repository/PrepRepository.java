package com.createarm.repository;

import com.createarm.model.Prep;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrepRepository extends JpaRepository<Prep, Integer> {
}
